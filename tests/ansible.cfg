[defaults]
# Le rôle est référencé par chemin relatif dans test.yml et
# test_env_encoding.yml ("{{ playbook_dir }}/.."), jamais par son nom :
# roles_path n'est donc pas nécessaire à sa résolution (voir README.md
# "Résolution du rôle").
host_key_checking = False
